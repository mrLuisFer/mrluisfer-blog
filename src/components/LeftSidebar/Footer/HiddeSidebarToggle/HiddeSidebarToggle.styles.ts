import styled from 'styled-components'
import { Button } from 'src/components/common/Button/Button.styles'

export const HiddeSidebarButton = styled(Button)`
  display: block;
  margin: 0 auto 1rem;
  width: 200px;
`
