import styled from 'styled-components'

export const RightSideTitle = styled.h2`
  text-align: center;
  margin-top: 4.5rem;
  margin-bottom: 0;
  opacity: 0.9;
  transition: var(--transition);
  font-weight: 600;

  &:hover {
    opacity: 1;
  }
`
